@{
    ViewData["Title"] = "Yazarlar";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-people"></i> Yazarlar</h2>
        <a href="/Authors/Create" class="btn btn-primary">
            <i class="bi bi-person-plus"></i> Yeni Yazar
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <div id="loading" class="text-center p-3">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Yükleniyor...</span>
                </div>
                <p class="mt-2 text-muted">Yazarlar yükleniyor...</p>
            </div>
            
            <div class="table-responsive">
                <table class="table table-hover d-none" id="authorsTable">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Yazar Adı</th>
                            <th>Biyografi</th>
                        </tr>
                    </thead>
                    <tbody id="tableBody">
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    const apiBase = "http://localhost:5045/api/authors";

    async function loadAuthors() {
        try {
            const response = await fetch(apiBase);
            const data = await response.json();
            
            const tbody = document.getElementById('tableBody');
            tbody.innerHTML = '';
            
            data.forEach(item => {
                const tr = document.createElement('tr');
                tr.innerHTML = `
                    <td>${item.id}</td>
                    <td><span class="fw-bold">${item.name}</span></td>
                    <td>${item.bio || '-'}</td>
                `;
                tbody.appendChild(tr);
            });
            
            document.getElementById('loading').classList.add('d-none');
            document.getElementById('authorsTable').classList.remove('d-none');
            
        } catch (error) {
            console.error('Hata:', error);
            document.getElementById('loading').innerHTML = '<div class="alert alert-danger">Veriler yüklenirken bir hata oluştu.</div>';
        }
    }

    document.addEventListener('DOMContentLoaded', loadAuthors);
</script>
